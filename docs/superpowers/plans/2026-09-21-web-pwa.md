# TheTotal web/PWA — vaihe 1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** TheTotal toimii selaimessa ja PWA:na Vercelissä ilman Supporter-ostoa.

**Architecture:** Sama Expo SDK 54 -koodipohja käännetään webiksi (react-native-web, `expo export -p web`, SPA). Natiivia ja webiä erottavat `Platform.OS === 'web'` -haarat pienissä apumoduuleissa (`lib/alert.ts`, `lib/shareImage.ts`), ei hajautettuina ruutuihin.

**Tech Stack:** Expo 54, expo-router 6, React 19.1, react-native-web, NativeWind 4, Zustand, Supabase, Jest (jest-expo), Vercel CLI.

**Spec:** `docs/superpowers/specs/2026-09-21-web-pwa-design.md`

## Global Constraints

- Repo `C:/Users/lauri/thetotal`, haara `feature/web`. Älä vaihda haaraa, älä commitoi masteriin.
- Commit: `git -c user.name=Lauri -c user.email=rekolalauri@gmail.com commit -m "<viesti>" -m "Co-Authored-By: <malli> <noreply@anthropic.com>"`. Älä muuta git-konfiguraatiota tai hookeja.
- Älä aja `eas update`, `eas build` tai `supabase`-komentoja, älä deployaa.
- Natiivikäytös ei saa muuttua: web-erot vain `Platform.OS === 'web'` -haaroissa.
- Käyttöliittymätekstit `lib/i18n.ts`:n kautta (fi + en), kuten nykyään.
- Ei uusia koodikommentteja. Ei uusia riippuvuuksia muita kuin Task 2:ssa nimetyt.
- Verifiointi jokaisessa taskissa: `npx tsc --noEmit` ja `npx jest` (nykyään vihreä) sekä Task 2:sta alkaen `npx expo export -p web --output-dir dist-web` onnistuu.
- `.env` ei koskaan commitoida eikä sen arvoja tulosteta.

---

### Task 1: Supporter/RevenueCat pois

**Files:**
- Delete: `lib/purchases.ts`, `components/profile/SupporterCard.tsx`, `supabase/functions/revenuecat-webhook/`, `supabase/add-supporter.sql`
- Modify: `app/(tabs)/profile.tsx` (import r.15, kortti r.454, `isSupporter` r.394 ja r.602), `app/user/[id].tsx` (select r.60 — poista `is_supporter`, prop r.350), `components/profile/BadgeRow.tsx` (supporter-merkki r.12, prop r.22, r.38), `store/userStore.ts` (kenttä r.22), `lib/i18n.ts` (kaikki `supporter.*` ja `profile.supportTitle/supportBody/supportButton`), `package.json` (`react-native-purchases` pois; aja `npm uninstall react-native-purchases`), `.env.txt` ja `.env`-kommentit eivät kuulu committiin — jätä ne, `TODO_JULKAISU.md` (poista osio A ja numeroi uudelleen; lisää alkuun rivi: "Supporter-osto poistettu 2026-09-21; sovellus on ilmainen.")
- Myös: jos `docs/*.html` tai `docs/app-store-texts.md` mainitsee Supporterin tai kertaoston, poista maininta.

**Interfaces:** Produces: `BadgeRow` ilman `isSupporter`-propia.

- [ ] Step 1: `grep -rniE "purchases|supporter|revenuecat" app components lib store docs supabase TODO_JULKAISU.md package.json` — listaa osumat.
- [ ] Step 2: Poista/muokkaa yllä olevat. Kun osio profiilissa poistuu, varmista ettei jää tyhjää otsikkoa tai väliä.
- [ ] Step 3: Grep uudelleen: ainoat sallitut osumat ovat `package-lock.json` ennen uninstallia (poistuu) ja tämä suunnitelma/spec.
- [ ] Step 4: `npx tsc --noEmit` ja `npx jest` vihreänä.
- [ ] Step 5: Commit `feat: remove the Supporter purchase`

### Task 2: Web-alusta, PWA ja natiivimoduulien vartijat

**Files:**
- Modify: `package.json`, `app.json`, `app/_layout.tsx`, `components/workout/SetRow.tsx`, `.gitignore`
- Create: `vercel.json`, `public/manifest.json` (tai Expon suosittelema vastine), tarvittaessa `public/index.html` / `app/+html.tsx`

**Interfaces:** Produces: `npm run build:web` → `dist-web/` staattinen SPA.

- [ ] Step 1: Asenna web-riippuvuudet Expon versiolukituksella: `npx expo install react-native-web react-dom @expo/metro-runtime`.
- [ ] Step 2: `app.json`: `platforms` += `"web"`; `web`: `{ "bundler": "metro", "output": "single", "favicon": "./assets/favicon.png", "name": "TheTotal", "shortName": "TheTotal", "themeColor": "#0d0d1a", "backgroundColor": "#0d0d1a", "display": "standalone", "orientation": "portrait" }`. Tarkista Context7:lla (`/expo/expo`, "web manifest PWA output single") mitkä kentät Expo 54 kirjoittaa manifestiin; jos ei kirjoita, luo `public/manifest.json` (name, short_name, start_url "/", display "standalone", background_color ja theme_color "#0d0d1a", icons: kopioi `assets/icon.png` → `public/icon.png` ja ilmoita se kokona "1024x1024", purpose "any") ja linkitä se sekä `apple-touch-icon`, `apple-mobile-web-app-capable=yes`, `apple-mobile-web-app-status-bar-style=black-translucent`, `theme-color` ja `viewport-fit=cover` HTML:ään Expon tukemalla tavalla (single-outputissa `public/index.html` -pohja; tarkista Context7:sta).
- [ ] Step 3: `package.json` scripts: `"build:web": "expo export -p web --output-dir dist-web"`. `.gitignore` += `dist-web/`.
- [ ] Step 4: `vercel.json`: `{ "buildCommand": "npm run build:web", "outputDirectory": "dist-web", "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }] }` — staattiset tiedostot palvellaan ennen rewritea.
- [ ] Step 5: `app/_layout.tsx`: `registerPushToken` ja kaikki `expo-notifications`-kutsut (myös kuuntelijat) ajetaan vain kun `Platform.OS !== 'web'`. Webissä juurinäkymä rajataan puhelinlevyiseksi: uloin näkymä `{ flex: 1, width: '100%', maxWidth: 480, alignSelf: 'center' }` vain webissä, taustaväri `#0d0d1a` koko sivulle.
- [ ] Step 6: `components/workout/SetRow.tsx`: Haptics-kutsut vain kun `Platform.OS !== 'web'`.
- [ ] Step 7: `npm run build:web` onnistuu. Aja `npx serve dist-web -s -l 4173` taustalla ja tarkista `curl -s localhost:4173/ | grep -i manifest` sekä `curl -s -o /dev/null -w "%{http_code}" localhost:4173/profile` = 200. Sammuta palvelin.
- [ ] Step 8: tsc + jest vihreänä. Commit `feat: build TheTotal for the web as an installable app`

### Task 3: Toimivat dialogit webissä

**Files:**
- Create: `lib/alert.ts`, `components/ui/AlertHost.tsx`, `__tests__/alert.test.ts`
- Modify: `app/_layout.tsx` (renderöi `<AlertHost />` kerran juureen), kaikki tiedostot joissa `Alert.alert` (grep: `lib/moderation.ts`, `app/(auth)/onboarding.tsx`, `components/workout/PRShareModal.tsx`, `components/workout/ExerciseBlock.tsx`, `components/ui/PRCommentsModal.tsx`, `components/profile/ShareRankModal.tsx`, `app/user/[id].tsx`, `app/(tabs)/start-workout.tsx`, `app/(tabs)/profile.tsx` ym.)

**Interfaces:**
- Produces:
```ts
export type AlertButton = { text: string; style?: 'default' | 'cancel' | 'destructive'; onPress?: () => void }
export type AlertRequest = { title: string; message?: string; buttons: AlertButton[] }
export function showAlert(title: string, message?: string, buttons?: AlertButton[]): void
export const useAlertStore: import('zustand').UseBoundStore<import('zustand').StoreApi<{ current: AlertRequest | null; queue: AlertRequest[]; push(r: AlertRequest): void; dismiss(): void }>>
```

- [ ] Step 1: Testi `__tests__/alert.test.ts` (mockaa `react-native`:n `Platform.OS = 'web'`): `showAlert('A')` → `current` = `{ title: 'A', buttons: [{ text: 'OK' }] }`; toinen `showAlert('B')` jonoutuu, `dismiss()` nostaa B:n; napin `onPress` kutsutaan kun AlertHost kutsuu `press(button)` (toteuta `press` storeen: kutsuu onPress ja dismiss). Natiivihaara: `Platform.OS = 'ios'` → `Alert.alert` kutsutaan samoilla argumenteilla eikä storeen tule mitään.
- [ ] Step 2: Aja, näe punaisena. Toteuta `lib/alert.ts` (zustand-store + showAlert). Aja vihreäksi.
- [ ] Step 3: `AlertHost`: renderöi vain webissä; `Modal transparent` tai absoluuttinen peitto, tumma kortti sovelluksen tyylillä (katso esim. `components/ui/RanksModal.tsx` värit/pyöristykset), otsikko, viesti, napit pystyyn (cancel viimeisenä, destructive punaisella), napit ≥44 px korkeita. Taustan napautus = cancel-napin onPress jos sellainen on, muuten sulje.
- [ ] Step 4: Korvaa jokainen `Alert.alert(` → `showAlert(` ja poista käyttämättömät `Alert`-importit. Grep `Alert.alert` = 0 osumaa `app components lib`-kansioissa (paitsi `lib/alert.ts`).
- [ ] Step 5: tsc + jest + build:web. Commit `feat: dialogs that work on the web`

### Task 4: Kuvakorttien jako ja datan vienti webissä

**Files:**
- Create: `lib/shareImage.ts`, `lib/download.ts`
- Modify: `components/profile/ShareRankModal.tsx`, `components/workout/PRShareModal.tsx`, `lib/dataExport.ts`

**Interfaces:**
- Produces: `export async function shareCapturedView(ref: React.RefObject<any>, fileName: string): Promise<void>` (lib/shareImage.ts) ja `export function downloadText(fileName: string, text: string, mime: string): void`, `export function downloadDataUrl(fileName: string, dataUrl: string): void` (lib/download.ts, vain web).

- [ ] Step 1: Selvitä tukeeko `react-native-view-shot` 4.0.3 webiä (`node_modules/react-native-view-shot/package.json` ja sen web-toteutus). Jos tukee, `captureRef(ref, { format: 'png', result: 'data-uri' })` webissä. Jos ei, asenna `html2canvas` ja kaappaa `ref.current` (react-native-webissä DOM-elementti).
- [ ] Step 2: `shareCapturedView`: natiivissa täsmälleen nykyinen logiikka (captureRef tmpfile + `Sharing.shareAsync`), siirrettynä modaaleista tähän. Webissä: data-URI → `File` (`image/png`); jos `navigator.canShare?.({ files: [file] })` → `navigator.share({ files: [file] })` (AbortError = käyttäjä perui, ei virhettä); muuten `downloadDataUrl`.
- [ ] Step 3: Modaalit käyttävät `shareCapturedView`:ia; virheet `showAlert(t('common.error'), e?.message ?? '')`.
- [ ] Step 4: `lib/dataExport.ts`: webissä `downloadText('thetotal-data.json', json, 'application/json')` ja palauta `true`; natiivi ennallaan.
- [ ] Step 5: tsc + jest + build:web. Commit `feat: share cards and export data on the web`

### Task 5: Julkaisu ja tarkistus (controller)

- [ ] Vercel-projekti `thetotal` CLI:llä (token Ostoslistan `.env.local`:sta), ympäristömuuttujat `EXPO_PUBLIC_SUPABASE_URL`, `EXPO_PUBLIC_SUPABASE_ANON_KEY` production-ympäristöön, `vercel deploy --prod`.
- [ ] Supabase Auth: `site_url` ja `uri_allow_list` Vercel-osoitteeseen Management API:lla (säilytä olemassa olevat natiivin redirectit).
- [ ] Testitili service rolella tai rekisteröitymällä, puhelinkokoinen tarkistus kaikista välilehdistä + dialogi + treenin tallennus; testidata pois.
- [ ] Loppukatselmointi (opus), korjausaalto, merge `feature/web` → master (koukku julkaisee natiiviin), push.
